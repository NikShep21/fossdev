[project]
name = "product-service"
version = "0.0.1"
description = "Product service for order service"
authors = [
    {name = "NikShep21",email = "klimenko.leva.lol2@yandex.ru"}
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi (>=0.136.1,<0.137.0)",
    "uvicorn[standard] (>=0.46.0,<0.47.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
